{
  "name": "ziyokor-nonlari",
  "version": "3.0.0",
  "private": true,
  "description": "ZIYOKOR NONLARI — P2P Decentralized Bread Marketplace",
  "scripts": {
    "build": "node build.js",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "build:apk": "node build.js && npx cap sync && cd android && ./gradlew assembleRelease",
    "build:debug": "node build.js && npx cap sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/geolocation": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0",
    "@capacitor/splash-screen": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/filesystem": "^7.0.0",
    "@capacitor/dialog": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/android": "^7.0.0"
  }
}
